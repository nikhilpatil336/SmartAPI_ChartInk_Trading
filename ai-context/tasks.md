# Tasks

## Current Task
— none —

## In Progress

## Recently Completed
- [x] **Session 34 — Test suite coverage gap fixes** — 52/52 passing. (1) `StopLossOpenStrategyTest` C7: full SL fill (delta=qty → remainingQty=0 → `modifySellOrder` and `placeCancelOrder` never called). (2) `ShortStopLossOpenStrategyTest` SC7: SHORT mirror — full SL fill → `modifyBuyOrder` and `placeCancelOrder` never called. (3) Added `longBuyOpenNotPartialFilled` factory method to `OrderContextFactory` — LONG mirror of `shortSellOpenNotPartialFilled`. (4) `BuyFilledOrderStrategyTest` Scenario 5: covers `!isBuyPartiallyFilled() && isBuyOpen()` guard at `BuyFilledOrderStrategy:344` — guard existed in production but had no test; LONG was asymmetric with SHORT (which had Scenario 5). CAS verified free after early return. (5) Added cleanup backlog entries for `SellOpenStrategy.java` (lines 61–324 dead) and `ShortTargetOpenStrategy.java` (lines 64–253 dead).
- [x] **Session 33 — Test suite second-pass audit** — 49/49 passing. (1) `SellOpenStrategyTest` C1/C2: added `verify(executionService, times(1)).modifyStopLossOrder(any(), any(), eq(3), ...)` — previously only verified `safeModifyOrReplace` was called, not the qty passed to `modifyStopLossOrder`. (2) Same `eq(3)` verify added to `ShortTargetOpenStrategyTest` SC1/SC2. (3) New C4 test (`fullSell_remainingQtyZero_noSlModify`): full fill delta=qty → remainingQty=0 → `safeModifyOrReplace` and `modifyStopLossOrder` never called. (4) New SC4 test (`fullBuy_remainingQtyZero_noSlModify`): same for SHORT target path. (5) Semantic fix: `shortEntryOpen(...)` replaced with `sellOpen(...)` in C1/C2/C3 (both produce identical objects but `sellOpen` is the correct name for LONG exit events).
- [x] **Session 32 — Test suite audit + fixes** — 47/47 passing. (1) `OrderStatusResponseFactory`: added `data.setStatus("complete"/"open")` to all 4 existing builders so responses are no longer byte-for-byte identical; added `sellComplete()` and `sellOpen()` factory methods for LONG exit scenarios. (2) `SellFilledOrderStrategyTest` + `StopLossFilledOrderStrategyTest`: replaced `shortEntryComplete(...)` with `sellComplete(...)` (same transactionType="SELL", semantically correct name). (3) `SquareOffManagerTest` D6: added SHORT no-fills scenario — tradebook empty → netQty=0 → `placeAggressiveExit` never called.
- [x] **Session 31 — Full Long+Short test coverage** — 46/46 passing across 13 test files. Added Short-side mirror tests for exit-open strategies (`ShortTargetOpenStrategyTest` SC1–SC3, `ShortStopLossOpenStrategyTest` SC4–SC6), SHORT netQty squareoff test (SquareOffManager D5), and cancel-fails error paths (E3 `ShortTargetFilledStrategyTest`, E4 `ShortStopLossFilledStrategyTest`). Added `shortReadyForTargetOpenFill` and `shortReadyForSlOpenFill` factory methods.
- [x] **Session 30 (extended) — Long-side exit coverage** — `SellOpenStrategyTest` C1–C3, `StopLossOpenStrategyTest` C4–C6, `SquareOffManagerTest` D1–D4, `SellFilledOrderStrategyTest` E1, `StopLossFilledOrderStrategyTest` E2. 37/37 pass.
- [x] **Session 30 — Order flow test coverage** — 25 unit tests across 8 test files covering all 4 entry strategies (CAS/delta/stale scenarios) + all 4 exit strategies (tradeCompleted guard) + ShortEntryFilledOrderStrategy unique guard. `./mvnw test` must pass before deploying to the Pi.

## Recently Completed
- [x] **Session 29 — CAS-before-delta bug fix in all 4 entry strategies** — Root cause: `tryBeginEntryOrders()` CAS was claimed **before** the `delta <= 0` guard in all 4 entry strategies. An `open` WS event with `filledshares=0` (order accepted by exchange, not yet filled) would claim the CAS and return early — permanently locking it. When the `complete` event arrived, the CAS was already taken so SL+target were never placed. Fix: moved `delta` computation and `delta <= 0` check to **before** `tryBeginEntryOrders()` in all 4 strategies. Files changed: `BuyOpenStrategy.java`, `ShortEntryOpenStrategy.java`, `BuyFilledOrderStrategy.java`, `ShortEntryFilledOrderStrategy.java`. Exit-side open strategies (`SellOpenStrategy`, `StopLossOpenStrategy`, `ShortTargetOpenStrategy`, `ShortStopLossOpenStrategy`) were confirmed clean — they use no CAS at all, just plain delta guards. Compile clean.
- [x] **Session 28 — EOD squareoff duplicate cancel fix + registry cleanup** — (1) `EODSquareOffScheduler` was iterating both `getAllBuyContexts()` and `getAllSellContexts()`, causing the same `OrderContext` to be squaredOff 2–3× per trade. Fixed by replacing both subscriptions with a single `Flux.fromIterable(getAllContextsSnapshot())` pass over `byBuyId` (single source of truth). (2) `SquareOffManager.squareOff()` guard changed from plain `ctx.getTradeCompleted().get()` read to `ctx.getTradeCompleted().compareAndSet(false, true)` CAS so concurrent calls can't both slip through. (3) `OrderEventDispatcher` "No handler for status" changed from ERROR → WARN (cancelled/open-pending are normal WS statuses during squareoff). (4) `WebClientConfig` — added shared `ConnectionProvider` with `maxIdleTime(10 min)` to both `directBrokerWebClient` and `proxyBrokerWebClient` to prevent 15-min AngelOne proxy idle-connection drops. (5) Enabled `orderRegistry.remove(ctx)` in all 4 fill strategies (`SellFilledOrderStrategy`, `StopLossFilledOrderStrategy`, `ShortTargetFilledStrategy`, `ShortStopLossFilledStrategy`) — called after `tradeCompleted.set(true)` in the success path only; inconsistent contexts (cancel failed) intentionally stay in registry. `OrderRegistry` injected into the 3 strategies that were missing it. Compile clean.
- [x] **Session 27 — Duplicate BUY+SL order bug fix (race condition)** — Root cause: `open` (partial fill) WS event and `complete` WS event arrived 3ms apart; both `ShortEntryOpenStrategy` and `ShortEntryFilledOrderStrategy` passed `supports()` check (`buyOrderId==null`) because `doOnSuccess` had not yet set `buyOrderId` when the second event was dispatched. Fix: added `AtomicBoolean entryOrdersPlacing` + `tryBeginEntryOrders()` CAS to `OrderContext` (same pattern as existing `tryStartExit()`). Added CAS guard at top of `onFilled` in all 4 entry strategies: `ShortEntryOpenStrategy`, `ShortEntryFilledOrderStrategy`, `BuyOpenStrategy`, `BuyFilledOrderStrategy`. First path to claim the CAS wins; the other immediately returns `Mono.empty()`. Compile clean.
- [x] **Session 26 — Analytics: Fix missing headers + human-readable MFE/MAE flags** — `ensureSheets()` now always calls `writeHeaderRow()` on all 3 sheets (new and existing), so new columns 58–67 headers are written even when the Excel file already exists. MFE/MAE milestone flag cells (cols 62–67) now write `"Yes"/"No"` strings instead of `1/0` integers. Compile clean. Run `POST /analytics/run-historical` to rebuild Excel.
- [x] **Session 25 — Analytics: Entry gap + MFE/MAE excursion columns** — Added 10 new columns to EOD analytics Excel (cols 58–67): `Entry Gap % (Next Open vs Alert Close)`, `Entry Gap Direction (ABOVE/BELOW/FLAT)`, `Max Favorable Move %`, `Max Adverse Move %`, and 6 milestone flags (`Reached +0.5/1.0/1.5% Favorable`, `Went -0.5/1.0/1.5% Against`) written as "Yes"/"No". Entry gap computed from next candle open vs alert candle close — available for all rows including NO_TRADE. MFE/MAE uses existing `evalCandles` (1-min if available, 5-min fallback) already fetched for TargetSlEvaluator — no extra API call. Compile clean. Run `POST /analytics/run-historical` to rebuild Excel.
- [x] **Session 24 — TargetSlEvaluator trailing SL squareoffPrice fix** — `squareoffPrice` was not set in the two subsequent-candle TRAILING_EXIT return paths (LONG block and SHORT block in the trailing-active loop). Same-candle path already set it correctly. Added `result.squareoffPrice = trailingSl` to both paths. Analytics Excel `squareoff_price` column now shows the correct exit price instead of 0 for these trades. BacktestRunner P&L was not affected (uses `trailingSlExitPrice`). Compile clean.
- [x] **Session 23 — Configurable live trading strategy** — Added `myapp.live-strategy` (default `BASELINE_VOL_SMA10`, set to `EMA_CROSS_V1` to switch), `myapp.live-ema-fast-period=9`, `myapp.live-ema-slow-period=21` to `ApplicationProperties` + `application.properties`. Added `calculateEMA()` to `OrderService_v2` (SMA seed, then standard EMA formula). `handleSecondAlert()` branches on `liveStrategy`: EMA_CROSS_V1 checks `close > EMA9 && close > EMA21` for LONG, `close < EMA9 && close < EMA21` for SHORT; returns `Mono.empty()` if EMA warmup data is insufficient. BASELINE_VOL_SMA10 path unchanged. Compile clean.
- [x] **Session 23 — Compounded return + monthly separator** — Added `compoundedReturnPct` to `BacktestSummary` + `MonthlyStats`; geometric product computed in `computeStats()` and `computeMonthlyStats()` (via `factorMap`). `BacktestReportWriter`: "Compounded Return %" column added to overall and monthly tables; capital return columns (₹10K etc.) use compounded values; 2 blank rows added after each month group in monthly table (`wroteAny` guard prevents blank rows for empty months). Compile clean.
- [x] **Session 22 — Backtest capital simulation + monthly breakdown** — (1) New `myapp.backtest-capital-amounts=10000,50000,100000,500000` config (both blocks). (2) SUMMARY sheet overall table gains N capital columns: "Return ₹10K", "Return ₹50K", etc. — `totalPnlAfterChargesPct / 100 × capital`. (3) SUMMARY sheet gains a second monthly table (5-row gap below overall): Month | Strategy | Trades | Win Rate % | P&L After Charges % | Nifty Return % | Return ₹X... per month. (4) `BacktestSummary.MonthlyStats` inner class + `computeMonthlyStats(niftyByMonth)` buckets trades by YYYY-MM. (5) `BacktestRunner.fetchNiftyReturn` replaced by 3 methods: `fetchNiftyCandles`, `computeOverallNiftyReturn`, `computeMonthlyNiftyReturns` — candles fetched once, both overall and monthly returns computed from same fetch. Compile clean.
- [x] **Session 21 — Alert trigger log + CURRENT_CLOSE backtest fix** — (1) New `AlertTriggerLogService` in `storage/`: logs every second alert as `Wed, May 27, 2026 2:20 PM|1|FEDERALBNK|288.85|289.40` to a configurable file; fire-and-forget via `Mono.fromRunnable().subscribeOn(Schedulers.boundedElastic())`; zero impact on trade path. Config: `myapp.alert-trigger-log-path` in both Windows + Pi blocks. (2) `CURRENT_CLOSE` backtest entry now uses the explicit first alert trigger price from Excel col D (via new `AlertEntry` record), not the candle close derived from the second-alert time. `BacktestController.readAlerts()` reads col D; `BacktestRunner.resolveEntry()` uses the hint when `> 0`, falls back to candle close when absent. Compile clean.
- [x] **Session 19 (cont.) — Dual Nifty benchmark in SUMMARY sheet** — Two parallel benchmarks: "Beats Nifty (Actual)?" uses real Nifty 50 1D candles fetched via API for the exact alert date range; "Beats Nifty (Avg Pace)?" uses 12.5% × (days/365) arithmetic benchmark — no API dependency. Both columns in SUMMARY sheet alongside "Nifty Return (Period) %" and "Nifty Avg Pace (Period) %". Compile clean.
- [x] **Session 19 (cont.) — Nifty benchmark in SUMMARY sheet** — Added `niftySymbolToken` to `ApplicationProperties` + `application.properties` (default "26000"). `BacktestRunner.run()` extracts min/max alert dates, fetches Nifty 50 1D candles for that exact range via `CandleCacheService`, computes `(endClose - startClose) / startClose × 100`. All summaries stamped with `niftyReturnPct`, `niftyDateRange`, `beatsNifty`. SUMMARY sheet gets 3 new columns: "Nifty Return (Period) %", "Nifty Period", "Beats Nifty?" (YES/NO). Gracefully skips if token blank or API fails. Compile clean.
- [x] **Session 19 (cont.) — Expectancy + maxDrawdown consistency fixes** — `expectancy` changed from `avgPnlPerTrade` (gross) to `avgPnlAfterChargesPct` (net) so it matches the verdict basis; `maxDrawdown` cumulative P&L now uses `pnlAfterChargesPct` instead of `pnlPct`; SUMMARY sheet headers updated ("Expectancy (Avg Net P&L%)", "Avg Gross P&L/Trade %"). Compile clean.
- [x] **Session 19 (cont.) — Trailing exit P&L bug fix** — `exitPrice` for WIN outcomes now branches on `trailingOutcome`: TRAILING_EXIT uses `eval.trailingSlExitPrice` (actual exit), all other WIN outcomes use `targetPrice`. Previously all WIN trades used `targetPrice`, causing trailing SL exits to under- or overstate P&L depending on how far the stock ran past target. Compile clean.
- [x] **Session 19 (cont.) — Backtest 5 bug fixes** — BREAKOUT entry now records NO_TRADE (with reason "breakout never triggered") instead of silently dropping; BREAKOUT scan stops at end of alert day so next-day candles can't falsely trigger; Sharpe ratio variance changed to sample variance (n-1 divisor); maxConsecutiveLosses now uses pnlAfterChargesPct to catch charge-negative squareoffs; TargetSlEvaluator trailing same-candle exit now sets squareoffPrice=trailingSl for consistent Excel output. Compile clean.
- [x] **Session 19 (cont.) — Backtest 3 more fixes** — VWAP confirmed already correct (daily reset already implemented). Added (stock, date) deduplication in BacktestRunner.run() — first alert per stock per day wins, re-entries skipped. Added Sharpe ratio to BacktestSummary (avgPnlAfterChargesPct / stdDev of per-trade P&L after charges); added column to SUMMARY Excel sheet. Compile clean.
- [x] **Session 19 — Backtest architecture audit + 7 fixes** — Fixed expectancy formula (was mixing R:R with lossRate fraction; now = avgPnlPerTrade, includes all outcomes); squareoffs now properly counted in winRate denominator and maxConsecutiveLosses; added maxDrawdown + maxConsecutiveLosses to BacktestSummary + SUMMARY sheet; added INSUFFICIENT_DATA verdict when tradesTaken < 30; fixed targetAtrPeriod bug (resolveTarget was always using slAtrPeriod for target ATR key); made MACD/Supertrend/BB params configurable per strategy via StrategyConfig; added fromDate/toDate query params to POST /backtest/run-historical for in-sample/OOS splits. Compile clean.

## Recently Completed
- [x] **Session 18 — Backtest validated + full charges feature** — Ran POST /backtest/run-historical; ATR strategies (SUPERTREND_V1, ADX_EMA_TREND_V1) now show correct non-zero SL/target after ATR context fix; P&L and charge columns confirmed in Excel output; BASELINE_VOL_SMA10 is highest win-rate strategy at 50.4%; ADX_EMA_TREND_V1 has highest expectancy (0.254) with fewest trades; EMA_CROSS_V1 highest raw P&L (16.22%) but low win rate (27.5%); all strategies LOSS-MAKING or marginal after charges at zero-brokerage rate.
- [x] **Session 18 (cont.) — Indian market charges + profitability verdict** — Added `backtestBrokeragePct` config field to `ApplicationProperties` + `application.properties` (default 0.0, configurable for flat-fee brokers). Per-trade charges computed in `BacktestRunner`: STT 0.025% sell, NSE exchange 0.00325% both sides, SEBI 0.0001% both sides, stamp duty 0.003% buy, GST 18% on brokerage+exchange+SEBI. `BacktestResult` gains `totalChargesPct` + `pnlAfterChargesPct`; `BacktestSummary` gains `totalPnlAfterChargesPct`, `avgPnlAfterChargesPct`, `verdict` (PROFITABLE/LOSS-MAKING). SUMMARY sheet + detail sheet both updated with new columns. Compile clean.
- [x] **Session 18 (cont.) — Backtest bugfixes + P&L + latestEntryTime** — Fixed critical ATR context bug (ATR_MULTIPLE strategies now correctly populate ATR in IndicatorContext); fixed hardcoded ATR_14 period in resolveTarget; fixed Supertrend ignoring period param; added `latestEntryTime` to StrategyConfig + enforcement in run() loop; added `exitPrice`/`pnlPoints`/`pnlPct` to BacktestResult + `totalPnlPct`/`avgPnlPerTrade` to BacktestSummary; added P&L columns to SUMMARY + detail sheets; fixed BB_RSI_REVERSAL_V1 long condition (CLOSE_BELOW_INDICATOR); updated long_all_0.5pct.json with `latestEntryTime: 14:30`. Compile clean.
- [x] **Session 18 — LONG_ALL_0.5PCT strategy + takeAllLong/takeAllShort support** — Added `takeAllLong`/`takeAllShort` boolean flags to `StrategyConfig`; updated `ConfigurableBacktestStrategy.decide()` to short-circuit condition evaluation when flag is true; reason string distinguishes "take-all-long" from "LONG conditions met" in report; created `long_all_0.5pct.json` (CURRENT_CLOSE entry, FIXED_PCT 0.5% SL + target). Compile clean.
- [x] **Session 17 — Backtest framework (Phase 1–5)** — Full `backtest/` package: disk-based candle cache per (symbol, date, timeframe), JSON-driven strategy configs (no recompile to add strategies), 12 indicators (RSI Wilder, EMA, SMA, SMA_VOL, VWAP intraday, ATR, MACD, BB, Supertrend, ADX, Stoch, CandleColor), `ConfigurableBacktestStrategy` evaluates all ops (LT/GT/CLOSE_ABOVE/VOL_GT etc.), multi-timeframe support with offset, `BacktestRunner` fans out N strategies × M alerts in one pass, `BacktestReportWriter` writes summary + per-strategy sheets sorted by expectancy, `BacktestController` (3 endpoints), 3 sample strategy JSONs. Compile clean.

## Recently Completed
- [x] **Session 16 — EOD analytics column restructure + 1-min exit detection** — Restructured analytics to 58 columns in exact user-defined order. `alertDirection` now shows trade direction (LONG/SHORT/NO_TRADE) instead of candle color. Added 5 `hit_first_candle_*` columns (OHLCV of the candle where target/SL was hit). `sheetFor()` updated to route by LONG/SHORT/NO_TRADE. `candle_body_pct`/`range_pct` moved to after win stats. `trade_taken` moved to after volume analysis block. `prev_candle_volume` now leads prev-candle group. Run `/analytics/run-historical` to rebuild existing Excel with new layout.
- [x] **Session 15 — ApplicationProperties field/getter/setter sync** — Audited all `myapp.*` keys in `application.properties` against `ApplicationProperties.java`. Added 6 missing fields (`eodAnalyticsInputAlertPath`, `eodAnalyticsOutputFile`, `eodAnalyticsCron`, `eodMarketCloseTime`, `analyticsSquareoffTime`, `ordercontextSaveIntervalMs`) and added getter/setter for those 6 plus 6 existing `fallbackLtpMock*` fields that had fields but no accessors.

## Recently Completed
- [x] **Session 14 — remainingQty fix + configurable tick size** — Fixed `SellOpenStrategy` and `ShortTargetOpenStrategy` to compute `remainingQty = ctx.getQuantity() - lastFilledQty` instead of broken counter. Made default tick size configurable via `myapp.default-tick-size`. Confirmed `assertSufficientMargin()` already live on both entry paths.
- [x] **Session 13 — Fixed scrip master NPE + key mismatch** — `ScripMasterService.filterOnlyEquityNse` was keying the map by `item.get("name")` (company name) but Chartink sends ticker symbols. Changed key to `item.get("symbol").replace("-EQ", "")` so `SBILIFE` etc. resolve correctly. Added null guard in `getTokenForName` (returns null + warn instead of NPE). Added default `tickSize=10` fallback in `handleSingleStockAlert`, `chartinkSimpleBuyOrder`, `chartinkSimpleSellOrder` so a missing scrip entry degrades gracefully instead of crashing.


## In Progress

## Next Tasks

### High Priority (correctness / memory risk)

- [x] **Fix `remainingQty` tracking in `SellOpenStrategy` and `ShortTargetOpenStrategy`** — replaced `ctx.longSellRemainingQty(delta)` / `ctx.shortBuyRemainingQty(delta)` with `ctx.getQuantity() - ctx.getLastSellFilledQty().get()` / `ctx.getLastBuyFilledQty().get()` so SL is correctly reduced on each partial target fill.

- [x] **Decide on `orderRegistry.remove(ctx)` after trade completes** — enabled `remove(ctx)` in all 4 fill strategies on the success path only. Inconsistent contexts stay in registry intentionally.

- [x] **Re-enable balance guard in `chartinkSimpleBuyOrder` / `chartinkSimpleSellOrder`** — already live as `assertSufficientMargin()` at lines 345 and 439. The old `assertSufficientFunds()` comments above are dead; the active guard is in place.

### Medium Priority (dead code / cleanup)

- [ ] **Delete `OrderService` (v1) and its orphan services** — `OrderService.java` is entirely dead except for `OrderController.getOrderStatus()` which calls it. `OrderValidationService` and `OrderPollingService` are referenced only from commented-out code inside v1. Steps: migrate `getOrderStatus()` to `OrderService_v2`, then delete all three files and clean `OrderController` import/field.

- [ ] **Purge the giant commented-out block in `OrderService_v2.java`** — lines ~86–683 contain: old v1 buy-order logic, two alternative quantity-calc implementations, a fully commented single-stock `handleAlert/handleFirstAlert/handleSecondAlert`, and multiple interim versions of each method. All active code starts after line 700. This block makes the file ~600 lines longer than it needs to be and is actively confusing to read.

- [ ] **Clean up `FnoUniverseService.java`** — file contains three commented-out class declarations (the entire class defined twice before the live version at line 154). Delete the dead versions. Keep only the active `public class FnoUniverseService` starting at line 154.

- [ ] **Clean up `BuyFilledOrderStrategy.java`** — lines 25–~300 are a commented-out first version of the entire class. The active class starts below it. Delete the commented version; keep only the live one.

- [ ] **Clean up `SellOpenStrategy.java`** — lines 61–324 are commented-out prior versions (264 lines). Only the live `onFilled` starting at line 327 matters. Delete the dead versions.

- [ ] **Clean up `ShortTargetOpenStrategy.java`** — lines 64–253 are commented-out prior versions (~190 lines). Delete the dead versions.

- [ ] **Clean up `OrderRegistry.java`** — three earlier implementations of `remove(OrderContext)` are commented out (lines ~40–43, ~120–126, ~204–207). Only the version at line 299 is live. Delete the dead implementations.

- [ ] **Delete `scheduleReconnectWithFreshToken()` in `OrderWebSocketConnector`** — documented as dead code in `decisions.md`; never called; is a duplicate of `scheduleReconnect()`. Safe to delete.

- [ ] **Remove old hardcoded cron comment in `EODSquareOffScheduler`** — lines 28–35 contain the old hardcoded `@Scheduled(cron = "0 0 15 * * MON-FRI")` version that was replaced by the config-driven `${myapp.squareoff-cron}` at line 37. Delete the commented block.

### Low Priority (feature decisions)

- [ ] **Decide fate of JWT periodic refresh** — `@Scheduled` is commented out at `TokenManager.java:96`. The refresh token endpoint was unreliable on AngelOne; current approach does startup-only login. Either document this as a permanent decision in `decisions.md` or re-enable when endpoint is stable.

- [ ] **Enable / remove the trading window gate** — `tradingWindowEnable=false` in `application.properties`; guard checks exist in `OrderController` at lines 53, 81, 124. Either configure time bounds and enable it for live trading, or delete the dead guards to reduce noise.

- [x] **Document + implement fallback LTP alert** — implemented in `OrderService_v2.triggerLtpFallback()`; idempotency via `AtomicReference<AlertState>.compareAndSet`; mock LTP mode added for weekend testing; documented in `decisions.md`.

- [ ] **`BracketOrderRequest.java` is unused** — no references found in active code; likely from an earlier bracket-order experiment. Confirm and delete if truly unreferenced.

### Backtest Enhancement (Session 20)

- [x] **Create 8 new strategy JSON variants in `D:/1 percent growth trading/backtest-strategies/`** — Phase 1 overlay experiments (JSON only, no code):
    - `baseline_long_only.json` — BASELINE minus shorts (counter-setup shorts removed)
    - `baseline_rr15.json` — 1.5:1 RR (target 0.75%, SL 0.5%)
    - `baseline_atr.json` — ATR-based SL+target instead of fixed pct
    - `baseline_vwap_confirm.json` — add CLOSE_ABOVE VWAP reclaim as 3rd filter (long only)
    - `ema_cross_adx.json` — EMA_CROSS + ADX>22 to cut low-quality trades / reduce drawdown
    - `ema_cross_mtf.json` — EMA_CROSS + 15M EMA21 alignment (higher-TF trend confirmation)
    - `vwap_reclaim_long.json` — new: green + close>VWAP + vol>SMA10, ATR 2:1 target
    - `rsi_momentum_long.json` — new: RSI(14)>55 + green candle, ATR target

- [x] **Phase 2 — Engine: faithful Chartink entry replication** — Added `PrevDayLevelIndicator` (PREV_DAY_LOW, PREV_DAY_HIGH, DAY_LOW, DAY_HIGH) and `CandleStructureIndicator` (CANDLE_RANGE_PCT, CANDLE_CLOSED_AT_HIGH, CANDLE_CLOSED_AT_LOW); wired into `BacktestRunner.computeIndicator()`; added LOW/HIGH ops to `ConfigurableBacktestStrategy`; updated `ConditionSpec` comment; created `spring_faithful.json` replicating all 5 Chartink filters. Compile clean.

## Completed Tasks
- [x] Session 14 — Made default tick size configurable: added `myapp.default-tick-size=10` to `application.properties`, added `BigDecimal defaultTickSize` field to `ApplicationProperties`, replaced 3 hardcoded `BigDecimal.TEN` fallbacks in `OrderService_v2` with `applicationProperties.getDefaultTickSize()`
- [x] Session 13 — Fixed scrip master NPE: key changed from `name` to `symbol.replace("-EQ","")` in `ScripMasterService`; null guards added in `getTokenForName` and all three `getTickSize()` call sites; default tickSize=10 fallback for unknown stocks
- [x] Session 11/12 — Fixed `ShortEntryOpenStrategy` and `BuyOpenStrategy`: target and SL orders now placed for `ctx.getQuantity()` (full order qty) instead of partial-fill delta; compile also fixed by adding 3 missing analytics config fields to `ApplicationProperties`
- [x] Bootstrap Session 1 — Created ai-context/project-overview.md and filled CLAUDE.md (stack, commands, structure, project-specific rules)
- [x] Bootstrap Session 2 — Created ai-context/architecture.md (folder tree, system design diagram, 4 key data flows, config management)
- [x] Bootstrap Session 3 — Created ai-context/decisions.md (auth, data storage, config, patterns, disabled features)
- [x] Bootstrap Session 4 — Scanned codebase for TODOs, dead code, commented features, known issues; populated real task backlog
- [x] Session 5 — Implemented fallback LTP alert for missed/delayed 2nd alerts; AtomicReference CAS for idempotency; mock LTP test mode; compile verified clean
- [x] Session 6 — Fixed fallback routing: `triggerLtpFallback` now calls `handleAlert` (full path) instead of `handleSecondAlert` directly; removed early CAS from fallback; CAS ownership moved to `handleSingleStockAlert`
- [x] Session 7 — Fixed multi-stock batch fallback leak: `AlertStateRegistry.cancelFallbackIfPending()` + `.doOnSuccess()` in `handleAlert()` cancels remaining stocks' timers when any stock in the batch places an order
- [x] Session 8 — EOD Analytics system: 5 new config keys in ApplicationProperties + application.properties; HistoricalDataService hardcoded path replaced; new analytics/ package with AlertAnalyticsRow (40 cols), TargetSlEvaluator, AlertAnalyticsService, AlertAnalyticsExcelWriter, EODAnalyticsScheduler; 2 new controller endpoints (/analytics/run-today, /analytics/run-historical); compile clean
- [x] Session 9 — EOD Analytics overhaul: 3 fixed sheets (LONG_TRADES/SHORT_TRADES/DOJI_TRADES) in single analytics.xlsx; alertDirection = candle color (not trade decision); trade_taken flag; first_alert_trigger_price = 1st candle OPEN; squareoff_reason + squareoff_price (15:00 candle OPEN); trailing SL analysis (activates at high≥target, trails 0.1% below max high); 10 new columns (candle_body_pct, candle_range_pct, vol_vs_sma10/20_ratio, risk_reward_ratio, time_of_day, day_of_week, trailing_*); stats formulas row 1; append+dedup on daily runs; rebuild on historical; compile clean
- [x] Session 9 (cont.) — 1-min candle hybrid evaluation: 5-min candles used for signal analysis (SMA/color/volume), separate ONE_MINUTE fetch from entry time to 15:30 used for TargetSlEvaluator (more accurate SAME_CANDLE resolution, trailing SL precision, time_to_hit_mins); automatic fallback to 5-min when 1-min unavailable (historical dates > 30 days); compile clean
- [x] Session 10 — Verified EOD analytics cron wiring (EODAnalyticsScheduler → AlertAnalyticsService.runForToday()); added 3rd analytics endpoint GET /analytics/run-for-date?date=YYYY-MM-DD to HistoricDataController exposing existing runForDate(LocalDate)
